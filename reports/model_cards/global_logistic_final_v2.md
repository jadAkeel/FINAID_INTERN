# Model card: final Logistic artifact v2

This is the final-training serialization of the existing global Logistic anchor, fit on all permitted eligible history before production origin 316. It uses the same causal feature and training-only preprocessing contract as the development anchor. It is used to create an unscored June 2026 ledger, not to claim production readiness or PDF milestone success.

Evidence and provenance are recorded in `artifacts/model_registry/final_training_v2.json` and `artifacts/model_registry/global_logistic_final_v2.joblib`.
