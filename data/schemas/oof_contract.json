{
  "name": "base_oof_prediction",
  "required_columns": [
    "run_id", "model_id", "model_version", "origin_position", "origin_date", "target_date", "indicator_id", "y_true", "p_up_raw", "predicted_direction", "eligible", "ineligibility_reason", "data_quality_ok", "fit_window", "feature_version", "data_hash", "config_hash", "seed", "runtime_seconds", "error_flag", "error_message"
  ],
  "grouping_key": ["origin_position", "origin_date"],
  "leakage_rule": "training origin positions must be strictly less than forecast origin position"
}
